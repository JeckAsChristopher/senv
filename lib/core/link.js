const { promptPassword, checkEnvFile, encryptBinary, decryptBinary, wipeMemory } = require("./configure");

module.exports = { promptPassword, checkEnvFile, encryptBinary, decryptBinary, wipeMemory };
